#!/usr/bin/python3
def weight_average(my_list=[]):
  if not my_list:
        return 0
    sum, div = 0, 0
    for i in my_list:
        sum += tup[0] * tup[1]
        div += tup[1]
    return (num / den)
